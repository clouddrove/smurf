# Direct dependencies for the MkDocs site under docs/sm.
#
# Only direct dependencies are pinned. Transitive packages resolve to the
# newest compatible release at install time, so security fixes land without
# a lockfile refresh. This file used to be a full `pip freeze` that nothing
# installed, which meant its stale pins produced Dependabot alerts for a
# build that never used them.
#
# Install with: pip install -r docs/sm/docs/requirements.txt

mkdocs-material==9.7.7
mkdocs-git-revision-date-localized-plugin==1.5.4
